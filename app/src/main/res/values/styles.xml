<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- this is for Custom inputLayout which changes the color of hint and color to bloodOrnage-->
    <style name="TextInputLayout" parent="Widget.Design.TextInputLayout">
        <item name="hintTextAppearance">@style/HintTextAppearance</item>
        <item name="errorTextAppearance">@style/ErrorTextAppearance</item>
    </style>

    <!-- this is for style of textinputlayout hint and error color -->
    <style name="ErrorTextAppearance" parent="TextAppearance.AppCompat.Caption">
        <item name="android:textColor">#ff0000</item>
        <item name="android:textSize">@dimen/text_size_14</item>
    </style>

    <style name="HintTextAppearance" parent="TextAppearance.AppCompat.Caption">
        <item name="android:textColor">@color/hint_color</item>
        <item name="android:textSize">@dimen/text_size_16</item>
    </style>

    <style name="styleHintText" parent="TextAppearance.Design.Hint">
        <item name="android:textSize">@dimen/text_size_14</item>
        <item name="android:textColor">@color/hint_color</item>
    </style>

    <style name="TextAppearanceTheme" parent="TextAppearance.AppCompat">

        <!-- Hint color and label color in FALSE state -->
        <item name="android:textColor">@color/hint_color</item>
        <item name="android:textSize">@dimen/text_size_16</item>

        <!-- Label color in TRUE state and bar color FALSE and TRUE State -->
        <item name="android:colorControlNormal">@color/hint_color</item>
        <item name="android:colorControlActivated">@color/colorAccent</item>
    </style>

    <style name="styleErrorText" parent="@android:style/TextAppearance">
        <item name="android:textColor">#ff0000</item>
        <item name="android:textSize">@dimen/text_size_12</item>
    </style>

    <style name="style_label" parent="android:Widget.TextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/text_size_14</item>
    </style>

    <style name="style_heading_label" parent="android:Widget.TextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">@dimen/text_size_22</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="style_action" parent="android:Widget.Button">
        <item name="android:background">@drawable/bg_action</item>
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
        <item name="android:layout_width">@dimen/action_width</item>
        <item name="android:layout_height">@dimen/action_signin_height</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/text_size_16</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
